spring:
  application:
    name: admin-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: false

---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888